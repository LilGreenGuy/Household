<nav id="navbar" class="navbar sticky-top navbar-expand-lg navbar-dark">
    <div class="container">
        <a class="navbar-brand" href="#">Household</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup"
            aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
            <div class="navbar-nav">
                <a class="nav-link <%= page_name === "Home" ? "active" : "" %>" href="/">Home</a>
                <% if(currentUser) {%>
                <a class="nav-link <%= page_name === "Household" ? "active" : "" %>" href="/household">Household</a>
                <a class="nav-link <%= page_name === "Living Expenses" ? "active" : "" %>" href="/living-expenses">Living Expenses</a>
            <% } %>
            </div>
            <div class="navbar-nav ms-auto">
        <% if(!currentUser) {%>
          <a class="nav-link <%= page_name === "Login" ? "active" : "" %>" href="/login">Login</a>
          <a class="nav-link <%= page_name === "Register" ? "active" : "" %>" href="/register">Register</a>
          <% } else {%>
            <a class="nav-link" href="/logout">Logout</a>
            <% } %>
            </div>
        </div>
    </div>
</nav>